# Event Start Record

- **Team ID:** `LSH26-T###` <!-- TODO: replace with your team's actual number, e.g. LSH26-T042 -->
- **Problem ID:** `P10`
- **Repository:** `lsh26-t###-p10` <!-- TODO: match the team number above exactly -->
- **Event start code:** `<START-CODE>` <!-- TODO: fill in the code given at event start -->
- **Repository created before release:** No <!-- TODO: change to "Yes" only if the repository genuinely existed before the problem set was released -->

## Material present before 6:00 PM

List every scaffold, template, configuration, asset or file already
present before the event started. Write `None` if the repository was
empty.

| Material | Source or original location | What was already present |
|---|---|---|
| None | N/A | Repository was empty |

<!--
TODO before pushing: this table must be truthful and specific. If the
team actually started from an existing scaffold, template, or a
previous project, name it here honestly — CLARIFICATIONS.md is explicit
that "a pre-existing solution that substantially implements the
selected problem's domain rules, calculations, data model, workflow or
required behaviour is prohibited," and this file is checked against
commit history. Generic framework boilerplate (e.g. "create-vite react-ts
template, unmodified") is fine to declare and does not itself violate
that rule — only leave "None" if the repository was genuinely empty.
-->

## Declaration

This file was added in the first event-work commit. The team will
preserve the repository history until results are announced.
